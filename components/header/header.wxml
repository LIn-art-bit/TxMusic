<view class="header">
  <view class="title">{{title}}</view>
  <view class="more">
    <block wx:if="{{hasMore}}">
      <view class="more-text" bindtap="moreClick">
        更多</view>
      <van-icon name="arrow"/>
    </block>
  </view>
</view>